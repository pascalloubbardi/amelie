<%= link_to "#{current_order.order_items.size} Articles, Total commande: ( #{number_to_currency current_order.subtotal, unit: "€", separator: ",", delimiter: "", format: "%n %u" } )".upcase, cart_path, class: "btn btn-link" %>
