<div class="well">

  <div class="row">
    <div class="col-xs-7">
      <h4><%= produit.nonproduit %></small></h4>
    </div>
    <div class="col-xs-5">
      
      <%= form_for order_item, remote: true do |f| %>
      <h4 class="text-right">Prix unitaire: <span style="color: green"><%= number_to_currency produit.price, unit: "€", separator: ",", delimiter: "", format: "%n %u"  %></span></h4>
        <div class="input-group">
          <%= f.number_field :quantity, value: 1, class: "form-control", min: 1 %>
          <div class="input-group-btn">
            <%= f.hidden_field :produit_id, value: produit.id %>
            <%= f.submit "Ajouter un article".html_safe, class: "btn btn-primary" %>
          </div>
        </div>
      <% end %>
    </div>
    
  </div>
</div>
